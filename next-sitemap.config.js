module.exports = {
    siteUrl: 'https://michalh.vercel.app', // Your website URL
    generateRobotsTxt: true, // (optional) Generate a `robots.txt` file
    // Exclude specific pages or paths if necessary
    exclude: ['/admin/*'], 
  };
  